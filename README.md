# Практика вопросов - Таймер

Веб-приложение для практики ответов на вопросы с таймером. Построено на React + Vite. Поддерживает загрузку вопросов из XLSX файлов и автоматическое сохранение данных в localStorage.

## 🚀 Технологии

- **React 19** - UI библиотека
- **Vite** - Сборщик и dev-сервер
- **XLSX** - Парсинг Excel файлов
- **CSS3** - Стилизация

## 📁 Структура проекта

```
quaqa/
├── src/
│   ├── components/          # React компоненты
│   │   ├── Header.jsx
│   │   ├── StatsBlock.jsx
│   │   ├── TimerDisplay.jsx
│   │   ├── QuestionBlock.jsx
│   │   ├── TopicsSelection.jsx
│   │   ├── LoadingStatus.jsx
│   │   ├── ErrorStatus.jsx
│   │   ├── Results.jsx
│   │   └── FormatExample.jsx
│   ├── utils/               # Утилиты
│   │   ├── storage.js       # Работа с localStorage
│   │   ├── xlsxLoader.js    # Загрузка XLSX
│   │   ├── questionManager.js # Управление вопросами
│   │   └── timer.js         # Форматирование времени
│   ├── App.jsx              # Главный компонент
│   ├── main.jsx             # Точка входа
│   └── index.css            # Глобальные стили
├── index.html               # HTML шаблон
├── vite.config.js           # Конфигурация Vite
├── package.json
└── README.md
```

## 🛠️ Установка и запуск

### Локальная разработка

```bash
# Установка зависимостей
npm install

# Запуск dev-сервера
npm run dev

# Сборка для production
npm run build

# Просмотр production сборки
npm run preview
```

## 📋 Формат файла с вопросами

Файл должен быть в формате `.xlsx` или `.xls` и содержать:

- **Любое количество листов** - каждый лист = отдельная тема
- **Столбцы на каждом листе:**
  - Номер (опционально)
  - Вопрос (обязательно)
  - Ответ (обязательно)
  - Объяснение (обязательно)

Названия столбцов могут быть на русском или английском языке:
- Вопрос / Question
- Ответ / Answer
- Объяснение / Explanation

## 🌐 Развертывание на GitHub Pages

### Автоматическое развертывание (рекомендуется)

1. Включите GitHub Pages в настройках репозитория:
   - Settings → Pages
   - Source: GitHub Actions

2. Обновите `vite.config.js`:
   ```js
   base: process.env.NODE_ENV === 'production' ? '/имя-репозитория/' : '/',
   ```
   Если репозиторий называется `username.github.io`, используйте `'/'`

3. Закоммитьте и запушьте изменения:
   ```bash
   git add .
   git commit -m "Setup GitHub Pages"
   git push
   ```

4. GitHub Actions автоматически соберет и задеплоит проект

### Ручное развертывание

```bash
# Сборка проекта
npm run build

# Файлы будут в папке dist/
# Загрузите содержимое dist/ в ветку gh-pages или используйте GitHub Actions
```

## ✨ Особенности

- ✅ Модульная архитектура на React
- ✅ Быстрая сборка с Vite
- ✅ Загрузка вопросов из XLSX файлов
- ✅ Автоматическое сохранение в localStorage
- ✅ Выбор тем для практики
- ✅ Таймер на каждый вопрос (60 секунд)
- ✅ Общий таймер практики
- ✅ Статистика результатов
- ✅ Адаптивный дизайн
- ✅ Автоматический деплой на GitHub Pages

## 📝 Использование

1. Откройте приложение в браузере
2. Загрузите XLSX файл с вопросами
3. Выберите темы для практики
4. Нажмите "Начать практику"
5. Отвечайте на вопросы, используя кнопки "Показать ответ" и "Показать объяснение"
6. Переходите к следующему вопросу кнопкой "Далее"
7. После завершения просмотрите результаты

## 🔧 Настройка

### Изменение базового пути для GitHub Pages

В `vite.config.js` измените:
```js
base: process.env.NODE_ENV === 'production' ? '/ваш-репозиторий/' : '/',
```

### Отключение автоматической загрузки questions.xlsx

В `src/App.jsx` закомментируйте или удалите блок загрузки из URL в `useEffect`.

## 📄 Лицензия

Проект создан для личного использования.
